@model CMS_SYSTEM.Models.Websites
@{
    ViewData["Title"] = "Delete";
}
@{
    var value = (Model.IsDeleted == true) ? "Acvtivate" : "Deacvtivate"; 
    var styleValue = (Model.IsDeleted == true) ? "success" : "danger"; 
}

<h1>@value</h1>


<h3>Are you sure you want to deactivate this website?</h3>
<div>
    <h4>Websites</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.CreatedBy)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.CreatedBy)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.DomainUrl)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.DomainUrl)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.WebsiteName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.WebsiteName)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />

        <input type="submit" value="@value" class="btn btn-@styleValue" /> |

        <a asp-action="Index">Back to List</a>
    </form>
</div>
