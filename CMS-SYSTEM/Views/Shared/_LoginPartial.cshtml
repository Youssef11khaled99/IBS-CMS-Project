@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager
    <link rel="stylesheet" href="~/css/site.css" />
    <link rel="stylesheet" href="~/css/style.css" />
    <link rel="stylesheet" href="~/css/userHome.css" />
    <link rel="stylesheet" href="~/css/ProfileIndex.css" />

    @if (SignInManager.IsSignedIn(User))
    {
        @*<li class="nav-item">
                <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @User.Identity.Name!</a>
            </li>
            <li class="nav-item">
                <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("userHome", "Profile", new { area = "" })">
                    <button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
                </form>
            </li>*@
    }
    else
    {
        <nav>
            <ul>
                <li style="float:left"><a asp-area="Identity" asp-page="/Account/Login" class="active fa"> CMS_SYSTEM </a></li>
                <li style="float:right"><a asp-area="Identity" asp-page="/Account/Register" class="fa"> Register </a></li>
                <li style="float:right"><a asp-area="Identity" asp-page="/Account/Login" asp-route-returnUrl="@Url.Action("userHome", "Profile")" class="fa fa-user"> Login </a></li>


            </ul>
        </nav>
        @*<li class="nav-item">
                <a class="nav-link text-light" asp-area="Identity" asp-page="/Account/Register">Register</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-light" asp-area="Identity" asp-page="/Account/Login" asp-route-returnUrl="@Url.Action("userHome", "Profile")">Login</a>
            </li>*@
    }